import { Box } from '@mui/material'
import React from 'react'

const Loading = () => {
        return (
            <Box style={{display:'grid',justifyContent:'center',alignItems:'center'}}><h2>Loading...</h2></Box>
        )

}

export default Loading